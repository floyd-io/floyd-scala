akka {
  loglevel = "INFO"
  actor {
    debug {
#      receive = on
#      autoreceive = on
#      lifecycle = on
    }
  }
}

floyd {
  database {
    hostname = "localhost"
    name = "floyd-test"
  }
}